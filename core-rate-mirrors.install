post_install() {
  systemctl daemon-reload
  systemctl enable core-rate-mirrors.timer
}

post_upgrade() {
  systemctl daemon-reload
  systemctl enable core-rate-mirrors.timer
}

pre_remove() {
  systemctl disable core-rate-mirrors.timer
}
